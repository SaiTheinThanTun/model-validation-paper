#sensitivity analysis constructor

#input: model text, var name, range (or a vector input), output name (eg. model output or incidence etc)
#output: modified model text to run the model with a loop with varying values (within a range or a vector input)
        # for the variable of interest

consens <- function(model,v,range,output){
  
}